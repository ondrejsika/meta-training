{
  "name": "meta-training",
  "private": true,
  "dependencies": {
    "prettier": "^2.0.5"
  },
  "scripts": {
    "prettier-check": "prettier --check .",
    "prettier-write": "prettier --write ."
  }
}
